---
"@mono/hooks-vue": minor
"@mono/ui-vue": patch
---

新增 `@mono/hooks-vue` 包：`createTitleManager` 标题管理器（override > route > app 三级合成到 `document.title`，`Title` 支持 `MaybeRefOrGetter` 响应式求值）；`@mono/ui-web` 更名为 `@mono/ui-vue`（分层轴从平台改为框架）。
