{
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    // web 场景包需要 DOM 类型（document/window 等）；base 不携带 DOM，避免污染 Node/工具域包
    "lib": ["ES2022", "DOM", "DOM.Iterable"]
  },
  "include": ["src"]
}
